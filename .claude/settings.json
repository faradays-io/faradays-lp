{
	"permissions": {
		"allow": [
			"Bash(pnpm:*)",
			"Bash(export PNPM_HOME=*)",
			"Bash(./node_modules/.bin/eslint:*)",
			"Bash(./node_modules/.bin/prettier:*)",
			"Bash(./node_modules/.bin/next:*)"
		]
	},
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "jq -r '.tool_input.command' | grep -qiE 'shadcn(@[^[:space:]]+)?[[:space:]]+add([[:space:]]|$)' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"shadcn component download — confirm before fetching.\"}}' || true",
						"statusMessage": "Checking for shadcn download"
					}
				]
			}
		]
	}
}
